function getAverage (a,b) {
    var average = (a + b)/2;
    console.log(average);
    return average;
}

getAverage(1,2);
var myResult = getAverage(1,2);
console.log("the average of my numbers is " + myResult);